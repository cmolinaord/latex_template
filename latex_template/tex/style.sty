\usepackage[latin1,utf8]{inputenc}
\usepackage[spanish,UKenglish]{babel}

% Math, symbols and SI units
%\usepackage{amsmath}
%\usepackage{amssymb}
%\usepackage{siunitx} % SI units \SI{value}{units}

% Other Packages
%\usepackage{textcomp} % Extra Symbols (euro, copyright,...)

% Tables
%\usepackage{multirow} % To allow \newline in cells of tables
%\renewcommand{\arraystretch}{1.5}
%\usepackage{longtable} % Used for tables longer than one page

% Page settings
\usepackage[top=2.5cm, bottom=4cm, left=2.5cm, right=2.5cm]{geometry}
\usepackage{setspace}
%\linespread{1.5}
%\setlength{\parindent}{0cm}
%\setlength{\parskip}{1em}
\usepackage{makeidx}
\newcommand*\emptyPage{\newpage\null\thispagestyle{empty}\newpage}
%\usepackage{emptypage} % Removes headers in pages that are empty
%\usepackage{pdfpages} % To include others PDF docs inside the main document
%\usepackage{lscape} % To put some pages in landscape orientation
%\usepackage{pdflscape} % To put some pages in landscape orientation instead of pdflatex

% Headding and footer
%\usepackage{fancyhdr}
%	\pagestyle{fancy}
%	\geometry{headheight=1.5cm, footskip=2cm}
%
%	\fancyhf{} % Clear header and footer
%	\fancyhead[LE,RO]{Carlos Molina}
%	\fancyhead[RE,LO]{\rightmark}
%	\renewcommand{\headrulewidth}{1pt} % Line width
%
%	\renewcommand{\footrulewidth}{1pt}
%	\fancyfoot[LO,RE]{\includegraphics[width=5cm]{svg/logo_eseiaat}}
%	\fancyfoot[LE,RO]{Page \thepage}

% Table of contents
%\setcounter{secnumdepth}{3}

% Enumerations and lists
%\usepackage{enumitem} % For enumeration and list formatting
%\setlist[itemize]{topsep = 3mm, itemsep = -0.5em}
%\setlist[enumerate]{topsep = 3mm, itemsep = -0.5em}

% Graphics
\usepackage{graphicx}
%\usepackage{subcaption}

% Hyperref
\usepackage{hyperref}
\hypersetup{
	colorlinks=true, %set true if you want coloured links
	linktoc=all,     %set to all if you want both sections and subsections linked
	linkcolor=blue,  %choose some color if you want links to stand out
	urlcolor=blue,
	citecolor=red,
	linktocpage,     % Add this if you want links in ToC just in the page numbers
	pdftex,          % PDF metadata
	pdfauthor={Carlos Molina Ordóñez},
	pdftitle={PDF title},
	pdfsubject={},
	pdfkeywords={},
	pdfproducer={Latex with hyperref},
	pdfcreator={pdflatex}
}

\usepackage[nottoc]{tocbibind}

% BibTeX
\bibliographystyle{doc/myIEEEtran}

% Useful personal commands
%\newcommand*\registred{\textsuperscript{\textregistered}}
%\newcommand*\trademark{\textsuperscript{\texttrademark}}

% chapters formatx
\usepackage{etoolbox}% http://ctan.org/pkg/etoolbox
\makeatletter
% \patchcmd{<cmd>}{<search>}{<replace>}{<success>}{<failure>}
% --- Patch \chapter
\patchcmd{\@makechapterhead}{50\p@}{\chapheadtopskip}{}{}% Space from top of page to CHAPTER X
\patchcmd{\@makechapterhead}{20\p@}{\chapheadsep}{}{}% Space between CHAPTER X and CHAPTER TITLE
\patchcmd{\@makechapterhead}{40\p@}{\chapheadbelowskip}{}{}% Space between CHAPTER TITLE and text
% --- Patch \chapter*
\patchcmd{\@makeschapterhead}{50\p@}{\chapheadtopskip}{}{}% Space from top of page to CHAPTER TITLE
\patchcmd{\@makeschapterhead}{40\p@}{\chapheadbelowskip}{}{}% SPace between CHAPTER TITLE and text
\makeatother
% Set new lengths
\newlength{\chapheadtopskip}\setlength{\chapheadtopskip}{0pt}
\newlength{\chapheadsep}\setlength{\chapheadsep}{10pt}
\newlength{\chapheadbelowskip}\setlength{\chapheadbelowskip}{15pt}
